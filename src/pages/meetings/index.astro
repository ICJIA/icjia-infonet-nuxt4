---
// astro/src/pages/meetings/index.astro
//
// Placeholder — no content source identified during Phase 4 audit:
//   - Strapi has no `meetings` entity (confirmed in live introspection)
//   - Legacy content/meetings/ is empty
//   - app/pages/meetings/ routes existed but rendered an empty list
// URL preserved to avoid 404s for any inbound links.
// Re-introduce data source in a future phase when meeting content is available.
// Skip meetings/[slug].astro — no slugs to generate.
import BaseLayout from '~/layouts/BaseLayout.astro';
import PageHeader from '~/components/PageHeader.astro';
import { gitLastModifiedISO } from '~/lib/gitDates';
---
<BaseLayout title="Meetings" breadcrumbTitle="Meetings" dateModified={gitLastModifiedISO('src/pages/meetings/index.astro')}>
  <div class="page-container">
    <PageHeader title="Meetings" />
    <p>No meetings to display.</p>
  </div>
</BaseLayout>
